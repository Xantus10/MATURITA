FROM node:24

WORKDIR /app

COPY . .

ARG VITE_BE_URL
ARG VITE_MS_TENANT
ARG VITE_MS_APP_ID

ENV VITE_BE_URL=$VITE_BE_URL
ENV VITE_MS_TENANT=$VITE_MS_TENANT
ENV VITE_MS_APP_ID=$VITE_MS_APP_ID

RUN npm install

RUN npm run build

EXPOSE 3000
